meta {
  name: Support ticket
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/api/v1/support
  body: json
  auth: none
}

headers {
  X-User-Id: {{userId}}
}

body:json {
  {
    "subject": "Where is my order?",
    "body": "It seems late."
  }
}
